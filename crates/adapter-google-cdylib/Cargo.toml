[package]
name = "adapter-google-cdylib"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Aperio — cdylib FFI shell for the Google adapter plugin: emits the #[no_mangle] C-ABI exports the desktop dlopen loader resolves (DESIGN.md section 20)."

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk.workspace = true
adapter-google-plugin = { path = "../adapter-google-plugin" }

[lints]
workspace = true
