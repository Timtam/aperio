[package]
name = "adapter-microsoft-graph-cdylib"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Aperio — cdylib FFI shell for the Microsoft Graph adapter plugin: emits the #[no_mangle] C-ABI exports the desktop dlopen loader resolves (DESIGN.md section 20)."

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk.workspace = true
adapter-microsoft-graph-plugin = { path = "../adapter-microsoft-graph-plugin" }

[lints]
workspace = true
