{
  "id": "com.aperio.sync-adapter-local",
  "name": "Aperio Local Filesystem",
  "version": "0.1.0",
  "plugin_type": "adapter",
  "capabilities": ["sync"],
  "abi_version": 3,
  "min_app_version": "0.1.0",
  "author": "Aperio Contributors",
  "description": "Mirrors the dataset to a filesystem directory — NAS share, USB stick, dev test fixture. It declares no adapter kind and no account schema of its own: the account that uses it is the built-in store, which declares both and routes here for the sync half.",
  "signed": false
}
