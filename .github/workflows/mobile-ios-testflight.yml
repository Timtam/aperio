name: Mobile iOS TestFlight

# Full headless iOS pipeline: build the cal-ffi XCFramework on a macOS runner,
# drop the fresh artifact into the Expo app, then run EAS Build + auto-submit to
# TestFlight. The only secret needed is EXPO_TOKEN (an Expo access token) — the
# Apple submit credentials (ASC API key) live on the EAS servers, so no Apple
# secrets are stored here.
#
# This replaces the local "vendor the >100MB libs -> commit-locally -> eas build
# -> soft-reset" dance: CI rebuilds the framework fresh every run, so neither
# the static libs nor the generated Swift bindings live in git. Both are
# gitignored under mobile/modules/cal-ffi/ios/ and arrive from the xcframework
# job below. A committed copy could only ever be stale — the last one lagged
# the Rust by some forty symbols for months, and nothing noticed, because no
# build ever read it.
#
# Trigger: manual only. From GitHub → Actions → "Mobile iOS TestFlight" → Run
# workflow, pick the branch to build. NOTE: the "Run workflow" button only shows
# once this file exists on the DEFAULT branch (main) — that's a GitHub rule for
# workflow_dispatch — so the file is kept on main too (it never runs on its own).
# Dispatching against feat/mobile-foundation checks out and builds that branch.

on:
  workflow_dispatch:
    inputs:
      profile:
        description: EAS build profile
        default: production
        type: choice
        options: [production, preview]
      submit:
        description: Auto-submit to TestFlight (production only)
        default: true
        type: boolean

permissions:
  contents: read

jobs:
  # 1) Build the iOS Rust artifact where a Mac is required. Identical to the
  #    cal-ffi-ios-xcframework workflow's steps; uploaded for the eas job below.
  xcframework:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Add iOS Rust targets
        run: rustup target add aarch64-apple-ios aarch64-apple-ios-sim x86_64-apple-ios

      # Names and presence only — a credential must never reach a build log, so
      # nothing here prints a value or even a length. Turns a later "still says
      # no credentials stored" into one glance instead of a hunt.
      - name: Which OAuth clients this build can bake in
        env:
          APERIO_OAUTH_WEBEX_CLIENT_ID: ${{ secrets.APERIO_OAUTH_WEBEX_CLIENT_ID }}
          APERIO_OAUTH_WEBEX_CLIENT_SECRET: ${{ secrets.APERIO_OAUTH_WEBEX_CLIENT_SECRET }}
          APERIO_OAUTH_GOOGLE_CLIENT_ID: ${{ secrets.APERIO_OAUTH_GOOGLE_CLIENT_ID }}
          APERIO_OAUTH_GOOGLE_CLIENT_SECRET: ${{ secrets.APERIO_OAUTH_GOOGLE_CLIENT_SECRET }}
          APERIO_OAUTH_MICROSOFT_CLIENT_ID: ${{ secrets.APERIO_OAUTH_MICROSOFT_CLIENT_ID }}
        run: |
          for v in APERIO_OAUTH_WEBEX_CLIENT_ID APERIO_OAUTH_WEBEX_CLIENT_SECRET                    APERIO_OAUTH_GOOGLE_CLIENT_ID APERIO_OAUTH_GOOGLE_CLIENT_SECRET                    APERIO_OAUTH_MICROSOFT_CLIENT_ID; do
            if [ -n "${!v:-}" ]; then echo "$v: present"; else echo "$v: MISSING"; fi
          done

      - name: Build cal-ffi static libs (release)
        env:
          # `crates/builtin-oauth/build.rs` bakes these in at RUST compile time
          # via `option_env!`, so they have to exist in the environment of the
          # cargo step — not merely in the repository's secrets. Adding them to
          # GitHub without wiring them here leaves the app reporting "no
          # credentials stored", which is exactly what it did.
          #
          # A secret that does not exist expands to the empty string, and
          # `build.rs` treats empty as absent: the build still succeeds, with
          # one provider fewer baked. So listing all of them costs nothing.
          APERIO_OAUTH_WEBEX_CLIENT_ID: ${{ secrets.APERIO_OAUTH_WEBEX_CLIENT_ID }}
          APERIO_OAUTH_WEBEX_CLIENT_SECRET: ${{ secrets.APERIO_OAUTH_WEBEX_CLIENT_SECRET }}
          APERIO_OAUTH_GOOGLE_CLIENT_ID: ${{ secrets.APERIO_OAUTH_GOOGLE_CLIENT_ID }}
          APERIO_OAUTH_GOOGLE_CLIENT_SECRET: ${{ secrets.APERIO_OAUTH_GOOGLE_CLIENT_SECRET }}
          APERIO_OAUTH_MICROSOFT_CLIENT_ID: ${{ secrets.APERIO_OAUTH_MICROSOFT_CLIENT_ID }}
        run: |
          cargo build -p cal-ffi --release --target aarch64-apple-ios
          cargo build -p cal-ffi --release --target aarch64-apple-ios-sim
          cargo build -p cal-ffi --release --target x86_64-apple-ios

      - name: Generate UniFFI Swift bindings
        run: |
          cargo build -p cal-ffi --release
          cargo run -p cal-ffi --features cli --bin uniffi-bindgen -- \
            generate --library target/release/libcal_ffi.dylib \
            --language swift --out-dir target/uniffi-swift

      - name: Assemble framework headers
        run: |
          mkdir -p target/xcf-headers
          cp target/uniffi-swift/cal_ffiFFI.h target/xcf-headers/
          cp target/uniffi-swift/cal_ffiFFI.modulemap target/xcf-headers/module.modulemap

      - name: Fat simulator static lib
        run: |
          mkdir -p target/ios-sim-release
          lipo -create \
            target/aarch64-apple-ios-sim/release/libcal_ffi.a \
            target/x86_64-apple-ios/release/libcal_ffi.a \
            -output target/ios-sim-release/libcal_ffi.a

      - name: Create XCFramework
        run: |
          rm -rf target/CalFfi.xcframework
          xcodebuild -create-xcframework \
            -library target/aarch64-apple-ios/release/libcal_ffi.a -headers target/xcf-headers \
            -library target/ios-sim-release/libcal_ffi.a -headers target/xcf-headers \
            -output target/CalFfi.xcframework

      - name: Stage artifact (XCFramework + Swift bindings)
        run: |
          mkdir -p out
          cp -R target/CalFfi.xcframework out/
          cp target/uniffi-swift/cal_ffi.swift out/

      - uses: actions/upload-artifact@v4
        with:
          name: cal-ffi-ios
          path: out/
          if-no-files-found: error

  # 2) Assemble the app with the fresh framework and run EAS Build. Runs on cheap
  #    Linux — EAS does the actual iOS compile on its own macOS infra, so this job
  #    only packages + uploads + waits.
  eas:
    needs: xcframework
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch the fresh framework artifact
        uses: actions/download-artifact@v4
        with:
          name: cal-ffi-ios
          path: mobile/modules/cal-ffi/ios

      # eas-cli never reads the git index. It clones with `--no-checkout` and
      # copies the WORKING TREE, filtered by the `.gitignore` files it finds
      # from the git root — the Android pipeline shipped an AAB without its
      # native library twice before that was understood (mobile-android-play.yml
      # tells the story). The framework and the Swift bindings are gitignored
      # on purpose, because a committed copy can only ever be stale, so their
      # rules have to leave before the archive is built — and loudly if they
      # do not.
      - name: Let the fresh framework into the EAS archive
        run: |
          # Fixed strings, not a regex: the rules carry slashes and dots.
          grep -vFx \
            -e '/modules/cal-ffi/ios/CalFfi.xcframework/' \
            -e '/modules/cal-ffi/ios/cal_ffi.swift' \
            mobile/.gitignore > mobile/.gitignore.new
          mv mobile/.gitignore.new mobile/.gitignore
          if grep -qE '^/modules/cal-ffi/ios/(CalFfi\.xcframework/|cal_ffi\.swift)$' mobile/.gitignore; then
            echo "an iOS artifact rule survived; the archive would ship without the framework" >&2
            exit 1
          fi

      - name: Ephemeral commit so the fresh framework is what eas-cli sees
        run: |
          git config user.email "ci@aperio.local"
          git config user.name "Aperio CI"
          # -f past the rules the step above removed from the copy eas-cli
          # reads. The index is not what ships; a commit naming both static
          # libs and the bindings is proof the artifact arrived whole, and
          # `--error-unmatch` fails on the first piece missing.
          git add -f mobile/modules/cal-ffi/ios
          git commit -m "ci: vendor fresh cal-ffi xcframework [not pushed]"
          git ls-files --error-unmatch \
            mobile/modules/cal-ffi/ios/cal_ffi.swift \
            mobile/modules/cal-ffi/ios/CalFfi.xcframework/ios-arm64/libcal_ffi.a \
            mobile/modules/cal-ffi/ios/CalFfi.xcframework/ios-arm64_x86_64-simulator/libcal_ffi.a \
            > /dev/null

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      # `npm ci` is exact on purpose, and that makes it sensitive to WHICH npm
      # wrote the lockfile: a lockfile written by one npm major can omit nested
      # entries another insists on, and the build then dies in 27 seconds with
      # "Missing: <pkg> from lock file" — twice, while this job ran Node 20
      # (npm 10) and development happened on Node 24 (npm 11). Node 24 above
      # matches the local toolchain, so the lockfile and this step now agree
      # by construction. Still, after touching mobile/package.json, verify
      # before pushing — the dry run IS this step:
      #
      #   cd mobile && npm ci --dry-run
      - name: Install JS deps
        working-directory: mobile
        run: npm ci

      # Before anything expensive: the committed UniFFI bindings and the two
      # native bridges have to agree with crates/cal-ffi about every method and
      # its arity. A mismatch otherwise surfaces minutes in, as a Kotlin or
      # Swift compile failure, with the build already paid for.
      - name: FFI bridges agree with the Rust host
        working-directory: mobile
        run: node scripts/check-ffi-bridges.mjs

      - name: Install eas-cli
        run: npm i -g eas-cli

      # What the app will ASK Apple for, printed before anything tries to sign
      # it. Entitlements are assembled by config plugins whose order decides the
      # outcome, and the only place that used to be visible was an Xcode error
      # 25 minutes later — which is how a silently-broken `withoutPushEntitlement`
      # went unnoticed until EAS enabled Push Notifications at the developer
      # portal on its own.
      #
      # `|| true` on purpose: this is diagnostics. A config command that trips
      # over something must not be what stops a build that would otherwise pass.
      - name: Show the entitlements this build will request
        working-directory: mobile
        run: npx expo config --type introspect | sed -n '/entitlements/,/}/p' || true

      - name: EAS build + submit
        working-directory: mobile
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          PROFILE="${{ github.event.inputs.profile || 'production' }}"
          SUBMIT=""
          if [ "${{ github.event.inputs.submit }}" != "false" ] && [ "$PROFILE" = "production" ]; then
            SUBMIT="--auto-submit"
          fi
          # Ask eas-cli for the archive it WOULD upload and look inside it —
          # the same code path as the build, so this cannot pass while the
          # build ships something else. The index check above only proves the
          # artifact arrived; this proves it survives the .gitignore filtering
          # eas-cli applies to the working tree. `build:inspect` takes
          # platform, profile, stage, output, force and verbose — and nothing
          # else; `--non-interactive` belongs to `build` and aborts this one.
          eas build:inspect --platform ios --profile "$PROFILE" \
            --stage archive --output "$RUNNER_TEMP/eas-archive" \
            --force
          for f in \
            cal_ffi.swift \
            CalFfi.xcframework/ios-arm64/libcal_ffi.a \
            CalFfi.xcframework/ios-arm64_x86_64-simulator/libcal_ffi.a; do
            path="$RUNNER_TEMP/eas-archive/mobile/modules/cal-ffi/ios/$f"
            if [ ! -s "$path" ]; then
              echo "$f is missing from the archive EAS would upload" >&2
              exit 1
            fi
            echo "archive carries $f: $(stat -c%s "$path") bytes"
          done
          echo "eas build --platform ios --profile $PROFILE $SUBMIT --non-interactive"
          eas build --platform ios --profile "$PROFILE" $SUBMIT --non-interactive
