import { describe, it, expect } from 'vitest';

/**
 * Phase 0 smoke test. Confirms the test pipeline runs.
 * Replaced by real unit tests from Phase 2 onward.
 */
describe('smoke', () => {
  it('arithmetic works', () => {
    expect(1 + 1).toBe(2);
  });
});
